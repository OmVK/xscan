/// Syncfusion license note.
///
/// XScan uses Syncfusion's commercial plugins (`syncfusion_flutter_pdf` +
/// `syncfusion_flutter_pdfviewer`). Since Syncfusion Flutter 18.3.0.x license
/// key registration is **no longer required** (the controls run unwatermarked
/// for commercial/community license holders), so there is nothing to register
/// here. See https://help.syncfusion.com/flutter/licensing/overview for the
/// current licensing terms.
class SyncSettings {
  /// Kept for call-site compatibility; the Syncfusion Flutter products no
  /// longer require (and no longer expose) a `SyncfusionLicense` API.
  static void register() {}
}